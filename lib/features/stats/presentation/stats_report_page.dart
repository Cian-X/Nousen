import 'package:flutter/material.dart';

class StatsReportPage extends StatelessWidget {
  const StatsReportPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
